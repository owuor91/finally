services:
  finally:
    build: .
    image: finally
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      FINALLY_DB_PATH: /app/db/finally.db
    volumes:
      - finally-data:/app/db

volumes:
  finally-data:
