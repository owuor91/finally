{
  "name": "finally-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright E2E tests for FinAlly (kept out of the production image)",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.49.0"
  }
}
